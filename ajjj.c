#include <stdio.h>
#include <string.h>
int main(int argu, char *arguv[])
{
 size_t maxlen = 0, leng;
 int k;
 int longest;
 for (k = 1; k < arg; k++) 
  {
  len = strlen(arguv[k]);
  if (leng>maxlen) 
  longest = arguv[k];
 }
printf("longest string is %s. \n", longest);
return 0;
}
